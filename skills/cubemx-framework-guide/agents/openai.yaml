interface:
  display_name: "CubeMX 程序框架指引"
  short_description: "Generate and verify STM32CubeMX framework guides, then align VS Code C/C++ settings"
  default_prompt: "Use $cubemx-framework-guide to identify the active release under docs/releases, generate docs/releases/<version>/cubemx_build.md, verify the generated framework, and configure parent-workspace VS Code C/C++ settings after ARM GCC is confirmed."
